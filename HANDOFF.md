> 把本文件**全文**粘贴到新窗口作为第一条消息即可。工作区：`C:\Users\27059\Desktop\自学agent`

# 交接：自学辅助 Agent 项目

## 0. 你要做的第一件事

读完本条消息后，**立刻读这三个文件**，再开始任何工作：
1. `README.md` —— 项目定调 + 资产地图 + 未解问题
2. `docs/00_错题本.md` —— **总原则 + 前置检查清单 + 已犯错误**（每次开新步骤前都要读）
3. `docs/01_项目定调.md` —— 用户原话与完整规格

---

## 1. ⛔ 总原则（最高优先级，不得绕过）

> **「任何新的工程化工作之前，先证明这个研究选择是必要的，并优先寻找已有工具、已有定义、已有评测协议；没有证据，不准把直觉直接变成代码或者标准进行落地。」**

- 任何提案要写进代码或标准文件，必须先在 `docs/00_错题本.md` §六 把状态从 **待证明** 改为 **有依据** 并写明出处。**状态不改，不许写进代码。**
- 违反代价有账可查（自造评估量表 → 一整轮实验作废，见 `docs/04`）。

---

## 2. 项目是什么

给一门课的自学学生一个**能讲解知识点 / 例题**的 Agent。

**以项目落地为第一目的，研究发论文为次。** 目标是一个**能跑的、最终要投入使用的 agent**。

### 三阶段路线

| 阶段 | 做什么 | 指标 |
|---|---|---|
| **一 冷启动** | 传入课件 → 知识点讲解 / 例题讲解。两个杠杆：**① 讲解方式优化 ② 知识图谱搭建** | 大部分人 **≈70% 满意度**，且**优于"同款 LLM 冷启动"** |
| **二 短期适配** | 用户注入偏好/反馈（**2–3 轮**）→ **条件-策略**适配 | 满意度提升 |
| **三 长期** | 长期交互中构建**学生画像** | 满意度进一步提升 |

**主指标 = 满意度**（不是学习增益）。

### 关键约束

- **约 3 个月**，无硬截止，**由浅入深**慢慢做
- 最终用于**离散数学**自学（课件后续提供）；当前暂用 **AIAA 2711** 课件作试验场
- **教材只是接口，重点是程序做好**；**不追求通用**（追求通用会乱）
- 不搭完整知识图谱，**只挑几章强相关的**
- 开发期**只有用户本人一个被试**；约 **200 名学生**要等明年春季投入使用后
- **满意度是产品指标，按定义主观**，不需要研究级效度；但它**不能**用来声称"讲解质量更高"

---

## 3. 一个关键的层级区分（用户提出，已定）

- **讲得全不全 = 决策层**：给谁、讲到什么范围、聚焦哪个重难点。**依赖知识图谱 + 对个人的判断**
- **讲得好不好 = 实现层**：决策定了之后，这段内容**怎么被说出来**

两者**不冲突**。同一个知识点，"讲全"和"只突破难点"是两种不同的"好"——**"讲得好"离开决策就没有验收标准。**

---

## 4. 已有的东西（别重造）

| 路径 | 是什么 |
|---|---|
| `knowledge_base/` | **AIAA 2711 课件知识库**：128 chunks（13 PDF / 132 页），来源可追溯到文件+页码 |
| `scripts/build_kb.py` | **课件 → 知识库**构建脚本（换课件时复用） |
| `scripts/transcribe_pages.py` | 无文本层 PDF 的**视觉转写**（断点续跑）。**注意：AIAA 2711 的正课 PDF 是矢量轮廓，无文本层**，必须走这条路 |
| `docs/02_教学策略框架_策略×条件.md` | **阶段二的核心依据**（未验证） |
| `docs/03_讲得好的证据与结论.md` | **阶段一的证据基础**：已确立的机制、可直接用的已验证判据、可复用方法 |
| `docs/refs/` | 原始文献与**原文逐字判据**（MQI 编码手册、Thurm、KP2022、AutoTutor） |
| `experiments/M0_rubric_calibration/report.md` | 已封存实验的结论 |

---

## 5. 已否决的路（别重走，详见 `docs/04`）

1. **自造评估量表** —— 代价最大。整轮实验作废。**已有量表在 `docs/03` §三，直接用。**
2. **信息充分性测试 / 归因探针** —— 结构性失败：**LLM 求解器不是学习者**，测不出"读者要付多少加工成本"。
3. **把内容覆盖度当成实现层的仪表** —— 层次放错，它属于决策层。
4. **选为"学生犯错补救"设计的仪表** —— 本项目主场景是概念讲解，不是习题补救。
5. **把"我没找到"说成"没有"** —— 存在性否定需要系统检索支撑。
6. **按类型给学生贴标签**（学习风格等）—— 无实证支持。注意：**阶段三的"学生画像"是另一回事**（从该用户的实际交互历史中积累，不是先验类型标签）。
7. **先造量表、后建系统** —— 顺序错。

---

## 6. 建议的第一步

> ⚠️ **不要先从"评估量表"入手。** 已经栽过一次。

1. **搭一个能跑的最小闭环**：课件 → 检索相关 chunk → LLM 生成讲解 → 用户读 → **打满意度分**
   - 产物是**基线**（"同款 LLM 冷启动"）。**没有基线，70% 这个数字没有意义。**
2. **再搭"最小知识结构"**：从 `knowledge_base` 里选**一章**，抽出知识点 + 前置关系 + 易错点
3. **然后才谈"讲解方式优化"**

---

## 7. 待确认的问题（见 `README.md` §五）

1. "同款 LLM 冷启动"基线具体怎么定义？
2. 满意度怎么量化（几分制？什么维度？多频繁）？
3. 70% 满意度怎么算？
4. 知识图谱选哪几章？
5. **阶段二的"条件"从哪来？我们没有答题日志**（学生以阅读 + 提问为主）
6. 阶段三的"学生画像"具体形态？

**开工前先把 1、2 两条和用户确认清楚**，否则后面全是返工。
